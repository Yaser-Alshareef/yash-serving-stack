### Lab 1 in Week 2 we install the weights of the Qwen model on CPU and compare different formats (fb16, etc.), and we see how many tokens for every format and compare it with the other formats.

---
### Lab 2 in Week 2 we use (FastAPI, Pydantic, OpenAI) so we can set up a server using Uvicorn and then send requests. We also alter the schemas to see what status codes we will get, like (422), which is from Pydantic.

---
### Lab 3 in Week 2 we have done a Dockerfile so we can run a container and then mount the model from a local drive or from Hugging Face; either will work fine. We also push the container to the registry (Docker Hub), and we test all endpoints after pulling and running it.

---
